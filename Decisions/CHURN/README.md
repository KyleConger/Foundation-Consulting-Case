# Type 1 — Churn (if-lost-to-X then Y)

**What this is.** If we lose the customer to X, then do Y. X is move-out, competitive FWA, competitive fiber, or involuntary/nonpay. The playbook already lives in `Decisions/decisions.md`. It does not fire until G1 — the quarterly net-loss-by-category tracker — exists and the cells are filled.

**Live constraint.** Tracker cells are empty on purpose. Company Internet is **−120k** (Q1 2026). The **54k** excess vs Comcast (**1.8×** residential Internet net-loss rate) is unallocated until the first fill. **−66k** is a *ceiling* on the next Internet print after a full 54k close — not the briefing plan.

**Will not.** Invent a mix. Fire a reason play from empty cells. Treat call-center codes as the file. Treat −66k as the course of action.

**Spine.** `Decisions/decisions.md` (G1 + dormant playbook). Do not duplicate it here.

**Lives here.** `ifxy-stock-signal.md` and `stock_signal_model.py` (churn-print test: enough to MATTER vs Q1, not a re-rate). Network-churn work stays in `Analysis/` and `Decisions/Decision Planning/` — pointer only. Customer-performance is an LOB scorecard, not a churn mix; it stays in `Analysis/`.
